import { BsColorDirective } from './bs-color.directive';

describe('BsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BsColorDirective();
    expect(directive).toBeTruthy();
  });
});
