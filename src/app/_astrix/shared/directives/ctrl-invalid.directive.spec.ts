import { CtrlInvalidDirective } from './ctrl-invalid.directive';

describe('CtrlInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
